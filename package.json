{
  "scripts": {
    "test": "cd tests/ && yarn cypress open",
    "check-lint": "eslint tests/cypress/integration/** tests/cypress/support/**",
    "fix-lint": "eslint tests/cypress/integration/** --fix",
    "prepare": "husky install"
  },
  "devDependencies": {
    "eslint": "^8.12.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-cypress": "^2.12.1",
    "eslint-plugin-import": "^2.25.4",
    "husky": "7.0.4"
  }
}